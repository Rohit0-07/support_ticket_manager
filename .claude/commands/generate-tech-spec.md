Generate a technical specification for `$ARGUMENTS`. Read `features/$ARGUMENTS/1_spec.md` as primary input. Output `features/$ARGUMENTS/2_tech_spec.md` with: Architecture, Python function signatures (with type hints and docstrings), Pydantic models, FastAPI endpoint contracts (all status codes), Spec-to-Component traceability table, Sequence diagram. May read existing code for naming conventions only.
